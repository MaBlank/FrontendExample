import { OneButtonPipe } from './one-button.pipe';

describe('OneButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new OneButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
